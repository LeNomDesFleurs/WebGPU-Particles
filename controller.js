function initController() {
    const imageFileLoader = document.getElementById("load-image");
}

const controller = {
    addFunction(f) {

    }
}

window.addEventListener('keydown', (event) => {
    if (event.key === 'm') {
        window.miniview = !window.miniview;
    }
});

document.addEventListener("DOMContentLoaded", () => {

    
});
